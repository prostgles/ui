import { makeToolUse, type Scenario } from "testAskLLM/utils";

export const receiptImport: Scenario = {
  firstMessage: "I need to import some pdf receipts",
  steps: [
    {
      content: "",
      tool: [
        makeToolUse("prostgles-ui", "ask_user_questions", {
          questions: [
            {
              type: "choice",
              question: "How should the import run by default?",
              suggestedAnswers: [
                "Preview extracted receipts first, then insert approved rows (recommended)",
                "Insert automatically without preview",
              ],
            },
            {
              type: "choice",
              question: "How should likely duplicates be handled?",
              suggestedAnswers: [
                "Skip likely duplicates (vendor + date + total + receipt_number)",
                "Insert all rows even if duplicates",
                "Update existing matching rows",
              ],
            },
            {
              type: "choice",
              question:
                "If a required field is missing from a PDF, what should happen?",
              suggestedAnswers: [
                "Skip that file and report it (recommended)",
                "Try best-effort inference and insert with notes",
                "Stop the whole run with an error",
              ],
            },
          ],
        }),
      ],
    },
    {
      tool: [
        makeToolUse("prostgles-ui", "create_agentic_workflow", {
          workflow_function_definition:
            'import { defineAgenticWorkflow } from "./defineAgenticWorkflow";\nimport { promises as fs } from "node:fs";\nimport path from "node:path";\n\nvoid defineAgenticWorkflow(\n  {\n    name: "Import PDF Receipts into receipts",\n    containerConfiguration: {\n      timeout: 600000,\n      readOnly: false,\n      internetAccess: "none",\n      memory: "1g",\n      cpus: "1",\n    },\n    databaseAccessDefinitions: {\n      mode: "custom",\n      tablePermissions: {\n        receipts: {\n          select: true,\n          insert: true,\n          update: true,\n        },\n      },\n    },\n    orchestrationTools: {\n      documents: {\n        get_document_text: 1,\n      },\n    },\n    userInput: {\n      sourcePaths: {\n        title: "PDF files or folders",\n        type: "file-or-folder-paths",\n        accessMode: "read",\n      },\n      importMode: {\n        title: "Import mode",\n        type: "enum",\n        values: ["preview_then_insert", "insert_automatically"],\n        defaultValue: "preview_then_insert",\n      },\n      duplicateHandling: {\n        title: "Duplicate handling",\n        type: "enum",\n        values: ["skip", "insert_all", "update_existing"],\n        defaultValue: "skip",\n      },\n      missingFieldHandling: {\n        title: "Missing required fields",\n        type: "enum",\n        values: ["skip_file", "infer_best_effort", "fail_run"],\n        defaultValue: "skip_file",\n      },\n      defaultCurrency: {\n        title: "Default currency if not found",\n        type: "enum",\n        values: ["USD"],\n        defaultValue: "USD",\n      },\n    },\n    agentDefinitions: {\n      receiptExtractor: {\n        prompt:\n          "You extract receipt data from OCR text. Return only what can be supported by text. Amounts must be numeric. Dates must be YYYY-MM-DD where possible. If uncertain, return null for that field.",\n        outputSchema: {\n          vendor_name: { type: "string", optional: true },\n          purchase_date: { type: "string", optional: true },\n          currency_code: { type: "string", optional: true },\n          subtotal: { type: "number", optional: true },\n          tax_amount: { type: "number", optional: true },\n          total_amount: { type: "number", optional: true },\n          receipt_number: { type: "string", optional: true },\n          payment_method: { type: "string", optional: true },\n          notes: { type: "string", optional: true },\n          confidence: { type: "number", optional: true },\n        },\n      },\n    },\n  },\n  async ({\n    tableHandlers,\n    agentHandlers,\n    orchestratorToolHandlers,\n    userInputValues,\n    setProgress,\n  }) => {\n    const toPathArray = (value: unknown): string[] => {\n      if (Array.isArray(value)) {\n        const out: string[] = [];\n        for (const item of value) {\n          if (typeof item === "string" && item.trim()) out.push(item);\n        }\n        return out;\n      }\n      if (typeof value === "string" && value.trim()) return [value];\n      return [];\n    };\n\n    const normalizeDate = (value: string | undefined): string | null => {\n      if (!value) return null;\n      const trimmed = value.trim();\n      if (!trimmed) return null;\n      if (/^\\d{4}-\\d{2}-\\d{2}$/.test(trimmed)) return trimmed;\n      const d = new Date(trimmed);\n      if (Number.isNaN(d.getTime())) return null;\n      return d.toISOString().slice(0, 10);\n    };\n\n    const normalizeAmount = (value: number | undefined): number | null => {\n      if (typeof value !== "number" || !Number.isFinite(value)) return null;\n      return Math.round(value * 100) / 100;\n    };\n\n    const scanPdfFiles = async (inputPaths: string[]): Promise<string[]> => {\n      const found: string[] = [];\n\n      const walk = async (p: string): Promise<void> => {\n        let stat;\n        try {\n          stat = await fs.stat(p);\n        } catch {\n          return;\n        }\n\n        if (stat.isDirectory()) {\n          const entries = await fs.readdir(p, { withFileTypes: true });\n          for (const entry of entries) {\n            const fullPath = path.join(p, entry.name);\n            if (entry.isDirectory()) {\n              await walk(fullPath);\n            } else if (entry.isFile() && entry.name.toLowerCase().endsWith(".pdf")) {\n              found.push(fullPath);\n            }\n          }\n          return;\n        }\n\n        if (stat.isFile() && p.toLowerCase().endsWith(".pdf")) {\n          found.push(p);\n        }\n      };\n\n      for (const p of inputPaths) {\n        await walk(p);\n      }\n\n      return found;\n    };\n\n    const sourcePaths = toPathArray(userInputValues.sourcePaths);\n    if (!sourcePaths.length) {\n      throw new Error("No source path provided. Please select at least one PDF file or folder.");\n    }\n\n    const importMode =\n      userInputValues.importMode === "insert_automatically"\n        ? "insert_automatically"\n        : "preview_then_insert";\n    const duplicateHandling =\n      userInputValues.duplicateHandling === "insert_all" ||\n      userInputValues.duplicateHandling === "update_existing"\n        ? userInputValues.duplicateHandling\n        : "skip";\n    const missingFieldHandling =\n      userInputValues.missingFieldHandling === "infer_best_effort" ||\n      userInputValues.missingFieldHandling === "fail_run"\n        ? userInputValues.missingFieldHandling\n        : "skip_file";\n    const defaultCurrency =\n      typeof userInputValues.defaultCurrency === "string" && userInputValues.defaultCurrency.length === 3\n        ? userInputValues.defaultCurrency\n        : "USD";\n\n    await setProgress(5, "Scanning input paths for PDF receipts...");\n    const pdfFiles = await scanPdfFiles(sourcePaths);\n    if (!pdfFiles.length) {\n      throw new Error("No PDF files found in the selected paths.");\n    }\n\n    await setProgress(10, `Found ${pdfFiles.length} PDF files. Starting extraction...`);\n\n    const previewRows: Record<string, unknown>[] = [];\n    const skippedMissing: { file: string; reason: string }[] = [];\n    const skippedDuplicates: { file: string; duplicateId: unknown }[] = [];\n    const extractionErrors: { file: string; error: string }[] = [];\n    let updatedCount = 0;\n    let insertedCount = 0;\n\n    type ReceiptInsertRow = Parameters<typeof tableHandlers.receipts.insertMany>[0][number];\n    const pendingInsertBatch: ReceiptInsertRow[] = [];\n\n    const flushInsertBatch = async (): Promise<void> => {\n      if (!pendingInsertBatch.length) return;\n      await tableHandlers.receipts.insertMany(pendingInsertBatch);\n      insertedCount += pendingInsertBatch.length;\n      pendingInsertBatch.length = 0;\n    };\n\n    for (const [index, filePath] of pdfFiles.entries()) {\n      const progress = 10 + Math.floor(((index + 1) / pdfFiles.length) * 85);\n      await setProgress(progress, `Processing ${index + 1}/${pdfFiles.length}: ${path.basename(filePath)}`);\n\n      try {\n        const buffer = await fs.readFile(filePath);\n        const fileAsBase64 = buffer.toString("base64");\n\n        const docText = await orchestratorToolHandlers.documents.get_document_text({\n          contentType: "application/pdf",\n          fileAsBase64,\n           do_ocr: true,\n          do_table_structure: true,\n        });\n\n        const extraction = await agentHandlers.receiptExtractor(\n          [\n            `File: ${filePath}`,\n            "Extract the receipt fields from the OCR text below.",\n            "If missing, return null-equivalent by omitting the field.",\n            "OCR TEXT:",\n            docText.slice(0, 40000),\n          ].join("\\n\\n"),\n        );\n\n        const vendorName =\n          typeof extraction.vendor_name === "string" ? extraction.vendor_name.trim() : "";\n        const purchaseDate = normalizeDate(extraction.purchase_date);\n        let subtotal = normalizeAmount(extraction.subtotal);\n        let taxAmount = normalizeAmount(extraction.tax_amount);\n        let totalAmount = normalizeAmount(extraction.total_amount);\n        const receiptNumber =\n          typeof extraction.receipt_number === "string" && extraction.receipt_number.trim()\n            ? extraction.receipt_number.trim()\n            : null;\n        const paymentMethod =\n          typeof extraction.payment_method === "string" && extraction.payment_method.trim()\n            ? extraction.payment_method.trim()\n            : null;\n        const notes =\n          typeof extraction.notes === "string" && extraction.notes.trim()\n            ? extraction.notes.trim()\n            : `Imported from ${filePath}`;\n        const currencyCodeCandidate =\n          typeof extraction.currency_code === "string" && extraction.currency_code.trim().length >= 3\n            ? extraction.currency_code.trim().toUpperCase().slice(0, 3)\n            : defaultCurrency;\n\n        if (missingFieldHandling === "infer_best_effort") {\n          if (totalAmount !== null && taxAmount !== null && subtotal === null) {\n            subtotal = Math.max(0, Math.round((totalAmount - taxAmount) * 100) / 100);\n          }\n          if (subtotal !== null && taxAmount === null && totalAmount !== null) {\n            taxAmount = Math.max(0, Math.round((totalAmount - subtotal) * 100) / 100);\n          }\n          if (subtotal !== null && taxAmount !== null && totalAmount === null) {\n            totalAmount = Math.round((subtotal + taxAmount) * 100) / 100;\n          }\n        }\n\n        const missingRequired =\n          !vendorName || !purchaseDate || subtotal === null || totalAmount === null;\n\n        if (missingRequired) {\n          const reason =\n            "Missing one or more required fields (vendor_name, purchase_date, subtotal, total_amount).";\n\n          if (missingFieldHandling === "fail_run") {\n            throw new Error(`${reason} File: ${filePath}`);\n          }\n\n          skippedMissing.push({ file: filePath, reason });\n          continue;\n        }\n\n        const subtotalValue = subtotal;\n        const totalAmountValue = totalAmount;\n        const taxAmountValue = taxAmount ?? 0;\n\n        if (\n          subtotalValue === null ||\n          totalAmountValue === null ||\n          subtotalValue < 0 ||\n          taxAmountValue < 0 ||\n          totalAmountValue < 0 ||\n          totalAmountValue < subtotalValue\n        ) {\n          skippedMissing.push({\n            file: filePath,\n            reason: "Amounts failed table constraints (non-negative and total >= subtotal).",\n          });\n          continue;\n        }\n\n        const duplicateFilter: Record<string, unknown> = {\n          vendor_name: vendorName,\n          purchase_date: purchaseDate,\n          total_amount: totalAmountValue,\n          receipt_number: receiptNumber,\n        };\n\n        const existing = await tableHandlers.receipts.find(duplicateFilter, {\n          limit: 1,\n          select: { id: 1 },\n        });\n\n        const row: ReceiptInsertRow = {\n          vendor_name: vendorName,\n          purchase_date: purchaseDate,\n          currency_code: currencyCodeCandidate,\n          subtotal: subtotalValue,\n          tax_amount: taxAmountValue,\n          total_amount: totalAmountValue,\n          receipt_number: receiptNumber,\n          payment_method: paymentMethod,\n          notes,\n          metadata: {\n            source_file: filePath,\n            extractor_confidence:\n              typeof extraction.confidence === "number" ? extraction.confidence : null,\n            imported_via_workflow: true,\n          },\n        };\n\n        previewRows.push({\n          file: filePath,\n          ...row,\n          duplicate_found: existing.length > 0,\n        });\n\n        if (importMode !== "insert_automatically") {\n          continue;\n        }\n\n        if (existing.length > 0 && duplicateHandling === "skip") {\n          skippedDuplicates.push({ file: filePath, duplicateId: existing[0]?.id });\n          continue;\n        }\n\n        if (existing.length > 0 && duplicateHandling === "update_existing") {\n          const existingId = existing[0]?.id;\n          if (existingId !== undefined && existingId !== null) {\n            await tableHandlers.receipts.update({ id: existingId }, row, { multi: false });\n            updatedCount += 1;\n            continue;\n          }\n        }\n\n        pendingInsertBatch.push(row);\n        if (pendingInsertBatch.length >= 10) {\n          await flushInsertBatch();\n        }\n      } catch (error) {\n        const message = error instanceof Error ? error.message : String(error);\n        extractionErrors.push({ file: filePath, error: message });\n      }\n    }\n\n    if (importMode === "insert_automatically") {\n      await flushInsertBatch();\n    }\n\n    await setProgress(98, "Finalizing import summary...");\n\n    console.log("=== Receipt Import Summary ===");\n    console.log(`Files scanned: ${pdfFiles.length}`);\n    console.log(`Rows extracted: ${previewRows.length}`);\n    console.log(`Skipped (missing/invalid): ${skippedMissing.length}`);\n    console.log(`Skipped (duplicates): ${skippedDuplicates.length}`);\n    console.log(`Extraction errors: ${extractionErrors.length}`);\n\n    if (importMode === "insert_automatically") {\n      console.log(`Inserted rows: ${insertedCount}`);\n      console.log(`Updated rows: ${updatedCount}`);\n    } else {\n      console.log(\n        "Preview mode: no DB writes were made. Re-run with importMode=insert_automatically to commit.",\n      );\n    }\n\n    if (previewRows.length) {\n      const previewSample = previewRows.slice(0, 20);\n      console.log("Preview sample (first 20 rows):");\n      console.log(JSON.stringify(previewSample, null, 2));\n    }\n\n    if (skippedMissing.length) {\n      console.log("Skipped files (missing/invalid required fields):");\n      console.log(JSON.stringify(skippedMissing.slice(0, 50), null, 2));\n    }\n\n    if (skippedDuplicates.length) {\n      console.log("Skipped duplicates:");\n      console.log(JSON.stringify(skippedDuplicates.slice(0, 50), null, 2));\n    }\n\n    if (extractionErrors.length) {\n      console.log("Files with extraction/runtime errors:");\n      console.log(JSON.stringify(extractionErrors.slice(0, 50), null, 2));\n    }\n\n    await setProgress(100, "Receipt import workflow completed.");\n  },\n);\n',
          workflow_function_definition_summary:
            "Imports PDF receipts from selected files/folders into the existing receipts table. It OCRs each PDF using the documents tool, extracts structured fields via an agent, checks duplicates (vendor+date+total+receipt_number), and supports two run modes: preview-only (default) or automatic insert. It also supports configurable duplicate and missing-field handling, provides progress updates, uses batched insertMany writes, and prints a clear summary with skipped/error files.",
        }),
      ],
    },
  ],
};
