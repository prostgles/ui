import { makeToolUse, type Scenario } from "testAskLLM/utils";
import { demoRestaurantName } from "utils/constants";

export const hygieneRatingsApiScenario = {
  firstMessage:
    "I need to get the hygiene ratings for each restaurant from the gov website",
  steps: [
    {
      tool: [
        makeToolUse("prostgles-ui", "get_tool_list", {}),
        makeToolUse("db", "get_existing_tables_schema", {
          tableNames: [],
          tableNameRegex: "",
        }),
        // makeToolUse("db", "count", {
        //   tableName: "restaurants",
        // }),
        // makeToolUse("db", "execute_readonly_sql", {
        //   sql: `SELECT r.id, r.name, r.address, a.postal_code, a.city FROM restaurants r LEFT JOIN addresses a ON a.id = r.address_id ORDER BY r.id LIMIT 10;`,
        // }),
      ],
    },
    {
      tool: [
        makeToolUse("db", "count", {
          tableName: "restaurants",
        }),
        makeToolUse("db", "execute_readonly_sql", {
          sql: `SELECT r.id, r.name, r.address, a.postal_code, a.city FROM restaurants r LEFT JOIN addresses a ON a.id = r.address_id ORDER BY r.id LIMIT 10;`,
        }),
      ],
    },
    {
      tool: [
        makeToolUse("prostgles-ui", "ask_user_questions", {
          questions: [
            {
              type: "choice",
              question: "How should I deliver the hygiene ratings?",
              suggestedAnswers: [
                "Create a new table and save ratings for all restaurants (recommended)",
                "Just return a preview report (no DB writes)",
              ],
            },
          ],
        }),
      ],
    },
    {
      tool: [
        makeToolUse("prostgles-ui", "create_agentic_workflow", {
          workflow_function_definition:
            'import { defineAgenticWorkflow } from "./defineAgenticWorkflow";\n\ntype FsaEstablishment = {\n  BusinessName?: string;\n  RatingValue?: string;\n  RatingDate?: string;\n  FHRSID?: number;\n  LocalAuthorityName?: string;\n  PostCode?: string;\n  AddressLine1?: string;\n  AddressLine2?: string;\n  AddressLine3?: string;\n  AddressLine4?: string;\n  geocode?: { latitude?: string; longitude?: string };\n  scores?: {\n    Hygiene?: number;\n    Structural?: number;\n    ConfidenceInManagement?: number;\n  };\n};\n\ntype FsaResponse = { establishments?: FsaEstablishment[] };\n\nconst normalizeText = (value: string): string =>\n  value.toLowerCase().replace(/[^a-z0-9]+/g, " ").replace(/\\s+/g, " ").trim();\n\nconst sleep = async (ms: number): Promise<void> => {\n  await new Promise((resolve) => {\n    setTimeout(resolve, ms);\n  });\n};\n\nconst computeMatchScore = (\n  restaurantName: string,\n  city: string,\n  postcode: string,\n  candidate: FsaEstablishment,\n): number => {\n  const rn = normalizeText(restaurantName);\n  const cn = normalizeText(candidate.BusinessName ?? "");\n  let score = 0;\n\n  if (rn.length > 0 && cn.length > 0) {\n    if (rn === cn) score += 70;\n    else if (rn.includes(cn) || cn.includes(rn)) score += 40;\n\n    const rt = new Set(rn.split(" ").filter(Boolean));\n    const ct = new Set(cn.split(" ").filter(Boolean));\n    let overlap = 0;\n    for (const token of rt) {\n      if (ct.has(token)) overlap += 1;\n    }\n    score += Math.min(20, overlap * 4);\n  }\n\n  const rp = normalizeText(postcode);\n  const cp = normalizeText(candidate.PostCode ?? "");\n  if (rp.length > 0 && cp.length > 0 && rp === cp) score += 30;\n\n  const cityNorm = normalizeText(city);\n  if (cityNorm.length > 0) {\n    const addr = normalizeText(\n      [candidate.AddressLine1, candidate.AddressLine2, candidate.AddressLine3, candidate.AddressLine4]\n        .filter((v): v is string => Boolean(v))\n        .join(" "),\n    );\n    if (addr.includes(cityNorm)) score += 12;\n  }\n\n  return score;\n};\n\nconst fetchFsaCandidates = async (\n  name: string,\n  city: string,\n  postcode: string,\n): Promise<FsaEstablishment[]> => {\n  const endpoint = "https://api.ratings.food.gov.uk/Establishments";\n  const queries: URL[] = [];\n\n  const q1 = new URL(endpoint);\n  q1.searchParams.set("name", name);\n  q1.searchParams.set("pageSize", "25");\n  if (postcode.trim().length > 0) q1.searchParams.set("address", postcode);\n  else if (city.trim().length > 0) q1.searchParams.set("address", city);\n  queries.push(q1);\n\n  const q2 = new URL(endpoint);\n  q2.searchParams.set("name", name);\n  q2.searchParams.set("pageSize", "25");\n  queries.push(q2);\n\n  const all: FsaEstablishment[] = [];\n\n  for (const query of queries) {\n    let response: Response | null = null;\n    let attempts = 0;\n\n    while (response === null && attempts < 4) {\n      const retry = attempts;\n      attempts += 1;\n      try {\n        const res = await fetch(query.toString(), {\n          headers: { "x-api-version": "2", Accept: "application/json" },\n        });\n        if (res.status === 429 || res.status >= 500) {\n          await sleep(300 * 2 ** retry);\n          continue;\n        }\n        response = res;\n      } catch {\n        await sleep(300 * 2 ** retry);\n      }\n    }\n\n    if (response === null || !response.ok) continue;\n    const data = (await response.json()) as FsaResponse;\n    for (const est of data.establishments ?? []) all.push(est);\n    if (all.length > 0) break;\n  }\n\n  return all;\n};\n\nvoid defineAgenticWorkflow(\n  {\n    name: "Fetch and store restaurant hygiene ratings (UK FSA)",\n    containerConfiguration: {\n      timeout: 14_400_000,\n      internetAccess: "bridge",\n      memory: "1g",\n      cpus: "1",\n    },\n    databaseAccessDefinitions: {\n      mode: "custom",\n      ddlStatements: `\nCREATE TABLE IF NOT EXISTS hygiene_ratings (\n  id BIGSERIAL PRIMARY KEY,\n  restaurant_id int4 NOT NULL REFERENCES restaurants(id) ON DELETE CASCADE,\n  business_name text,\n  rating_value text,\n  rating_value_desc TEXT GENERATED ALWAYS AS (trim(lower(rating_value)) || \' - \' || CASE trim(lower(rating_value)) WHEN \'5\' THEN \'very good\'   WHEN \'4\' THEN \'good\'  WHEN \'3\' THEN \'generally satisfactory\'  WHEN \'2\' THEN \'some improvement necessary\'  WHEN \'1\' THEN \'major improvement necessary\'  WHEN \'0\' THEN \'urgent improvement necessary\'  ELSE trim(lower(rating_value)) END) STORED,\n  hygiene_score int4,\n  structural_score int4,\n  confidence_in_management_score int4,\n  rating_date date,\n  fhrsid int8,\n  local_authority_name text,\n  match_confidence numeric(5,2),\n  match_method text,\n  source_url text NOT NULL DEFAULT \'https://api.ratings.food.gov.uk/Establishments\',\n  fetched_at timestamptz NOT NULL DEFAULT now(),\n  status text NOT NULL DEFAULT \'matched\',\n  error_message text,\n  latitude numeric,\n  longitude numeric,\n  geog geography GENERATED ALWAYS AS (\n    CASE\n      WHEN latitude IS NOT NULL AND longitude IS NOT NULL\n      THEN st_point(longitude::double precision, latitude::double precision, 4326)::geography\n      ELSE NULL::geography\n    END\n  ) STORED,\n  api_payload jsonb,\n  CONSTRAINT hygiene_ratings_restaurant_id_key UNIQUE (restaurant_id)\n);\n      `,\n      tablePermissions: {\n        restaurants: { select: true },\n        addresses: { select: true },\n        hygiene_ratings: { select: true, insert: true, update: true },\n      },\n    },\n  },\n  async ({ tableHandlers, setProgress }) => {\n    const apiSourceUrl = "https://api.ratings.food.gov.uk/Establishments";\n    const pageSize = 250;\n\n    await setProgress(1, "Loading processed restaurants...");\n\n    const existing = await tableHandlers.hygiene_ratings.find(\n      {},\n      { select: { restaurant_id: 1 }, limit: 500000 },\n    );\n    const processed = new Set<number>();\n    for (const row of existing) processed.add(row.restaurant_id);\n\n    const total = await tableHandlers.restaurants.count({});\n    await setProgress(3, `Restaurants in DB: ${total}. Already processed: ${processed.size}.`);\n\n    let offset = 0;\n    let doneThisRun = 0;\n\n    while (offset < 5 && offset < total) {\n      const restaurants = await tableHandlers.restaurants.find(\n        { $or: ["' +
            demoRestaurantName +
            '", "McDonald","KFC","Burger King","Subway","Domino","Pizza Hut","Starbucks","Costa","Pret","Greggs","Nando"].map(v => ({ name: { $ilike: `%${v}%`} })) },\n        {\n          select: { id: 1, name: 1, address_id: 1 },\n          orderBy: { key: "id", asc: true },\n          limit: pageSize,\n          offset,\n        },\n      );\n      if (restaurants.length === 0) break;\n      offset += restaurants.length;\n\n      const addressIds = Array.from(new Set(restaurants.map((r) => r.address_id)));\n      const addresses = await tableHandlers.addresses.find(\n        { id: { $in: addressIds } },\n        { select: { id: 1, city: 1, postal_code: 1 }, limit: addressIds.length + 20 },\n      );\n      const byAddressId = new Map<string, { city: string; postal_code: string }>();\n      for (const addr of addresses) {\n        byAddressId.set(addr.id, { city: addr.city, postal_code: addr.postal_code });\n      }\n\n      for (const restaurant of restaurants) {\n        if (processed.has(restaurant.id)) continue;\n\n        const address = byAddressId.get(restaurant.address_id);\n        const city = address?.city ?? "";\n        const postcode = address?.postal_code ?? "";\n\n        let status = "not_found";\n        let businessName: string | null = null;\n        let ratingValue: string | null = null;\n        let hygieneScore: number | null = null;\n        let structuralScore: number | null = null;\n        let cimScore: number | null = null;\n        let ratingDate: string | null = null;\n        let fhrsid: number | null = null;\n        let localAuthority: string | null = null;\n        let matchConfidence: number | null = null;\n        let latitude: number | null = null;\n        let longitude: number | null = null;\n        let payload: FsaEstablishment | null = null;\n        let errorMessage: string | null = null;\n\n        try {\n          const candidates = await fetchFsaCandidates(restaurant.name, city, postcode);\n\n          let best: FsaEstablishment | null = null;\n          let bestScore = -1;\n          for (const candidate of candidates) {\n            const score = computeMatchScore(restaurant.name, city, postcode, candidate);\n            if (score > bestScore) {\n              best = candidate;\n              bestScore = score;\n            }\n          }\n\n          if (best !== null && bestScore >= 35) {\n            const lat = best.geocode?.latitude ? Number(best.geocode.latitude) : null;\n            const lon = best.geocode?.longitude ? Number(best.geocode.longitude) : null;\n\n            status = "matched";\n            businessName = best.BusinessName ?? null;\n            ratingValue = best.RatingValue ?? null;\n            hygieneScore = best.scores?.Hygiene ?? null;\n            structuralScore = best.scores?.Structural ?? null;\n            cimScore = best.scores?.ConfidenceInManagement ?? null;\n            ratingDate = best.RatingDate ? best.RatingDate.slice(0, 10) : null;\n            fhrsid = best.FHRSID ?? null;\n            localAuthority = best.LocalAuthorityName ?? null;\n            matchConfidence = bestScore;\n            latitude = Number.isFinite(lat) ? lat : null;\n            longitude = Number.isFinite(lon) ? lon : null;\n            payload = best;\n          } else {\n            status = "not_found";\n            matchConfidence = bestScore < 0 ? 0 : bestScore;\n          }\n        } catch (error) {\n          status = "error";\n          errorMessage = error instanceof Error ? error.message.slice(0, 500) : "Unknown error";\n        }\n\n        await tableHandlers.hygiene_ratings.insert(\n          {\n            restaurant_id: restaurant.id,\n            business_name: businessName,\n            rating_value: ratingValue,\n            hygiene_score: hygieneScore,\n            structural_score: structuralScore,\n            confidence_in_management_score: cimScore,\n            rating_date: ratingDate,\n            fhrsid,\n            local_authority_name: localAuthority,\n            match_confidence: matchConfidence,\n            match_method: "name+address",\n            source_url: apiSourceUrl,\n            fetched_at: new Date().toISOString(),\n            status,\n            error_message: errorMessage,\n            latitude,\n            longitude,\n            api_payload: payload,\n          },\n          { onConflict: "DoUpdate", returning: { restaurant_id: 1 } },\n        );\n\n        processed.add(restaurant.id);\n        doneThisRun += 1;\n\n        if (doneThisRun % 25 === 0) {\n          const complete = processed.size;\n          const progress = Math.min(99, Math.round((complete / total) * 100));\n          await setProgress(\n            progress,\n            `Processed ${doneThisRun} in this run. Total complete ${complete}/${total}.`,\n          );\n        }\n      }\n    }\n\n    await setProgress(100, `Done. Processed ${doneThisRun} restaurants in this run.`);\n  },\n);\n',
          workflow_function_definition_summary:
            "Creates a new `hygiene_ratings` table, then fetches UK Food Standards Agency ratings for each restaurant, matches results by name + postcode/city, and upserts one rating row per restaurant. It is rerun-safe (skips already processed rows), stores confidence/status/raw payload, and includes a generated PostGIS geography column from API coordinates for map views.",
        }),
      ],
    },
  ],
} as const satisfies Scenario;
