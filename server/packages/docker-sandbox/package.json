{
  "name": "docker-sandbox-mcp-server",
  "version": "1.0.0",
  "description": "MCP server for executing code in Docker containers",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "test": "node --test dist/*.spec.js",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.1",
    "prostgles-server": "^4.2.323"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "files": [
    "dist/"
  ]
}
